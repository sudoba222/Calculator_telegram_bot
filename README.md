### 1. install these inside your terminal : 
1.a)  ->if mac or linux <pre>python -m venv venv</pre> 
2.a)  ->if mac or linux <pre>source venv/bin/activate</pre> 
1.b)  -> if windows <pre>python -m venv .venv</pre> 
2.b)  -> if windows
    <pre>.venv\Scripts\activate </pre> 

### 2. Install these libraries : 
1) <pre>pip install python-telegram-bot --upgrade</pre>
2) <pre>install python-dotenv</pre>

### 3. BOT Connection: 
1) create a bot in telegram BOT FAther
2) then after creation copy the token given for the bot
3) then  we need to store our bot token. Create a file named .env in your project folder and add your token.
   <pre>BOT_TOKEN="YOUR_TOKEN"</pre>

